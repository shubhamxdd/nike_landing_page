import { ReviewCardProps } from "@/types";

const ReviewCard = ({
  customerName,
  feedback,
  imgURL,
  rating,
}: ReviewCardProps) => {
  return (
  <div>
    
  </div>
  );
};

export default ReviewCard;
